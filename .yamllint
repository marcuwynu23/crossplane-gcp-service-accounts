---
extends: default
rules:
  line-length:
    max: 120
  document-start:
    present: true
  truthy:
    check-keys: false
  # Quoted strings like "${{ github.event.inputs.region }}" in GitHub
  # Actions workflows are intentional; allow them.
  quoted-strings:
    required: false
